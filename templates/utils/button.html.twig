{# <div class="btn-toolbar d-none d-sm-block" role="toolbar" aria-label="Toolbar with button groups"> #}
    {# <div class="btn-group m-1" role="group" aria-label="First group"> #}
       <div class="mod"> <a href={{ path(path, { id:id, (type)~'_id': file }) }} type="button" class="btn btn-primary btn-sm">✍️</a></div>
    {# </div> #}
    {% if display is defined %}
        {# <div class="btn-group m-1" role="group" aria-label="Second group"> #}
            <div class="del"><button  type="button" class="btn btn-danger btn-sm del" data-toggle="modal" data-target="#deleteModal{{file}}">🗑️</button></div>
        {# </div> #}
    {% endif %}
{# </div> #}