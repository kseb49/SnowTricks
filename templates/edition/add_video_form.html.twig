{% extends 'base.html.twig' %}

{% block title %}Ajouter des videos{% endblock %}

{% block body %}
    <div class="container">
        <h1>Ajouter des videos</h1><br>
            {{ form_start(add_video_form) }}
                {{ form_errors(add_video_form) }}
                {{ form_row(add_video_form.videos)}}
                {# <button type="button" class="btn btn-primary" id="addVideo">Ajouter une autre video</button> #}
                <button type="submit" class="btn btn-primary">Ajouter</button>
            {{ form_end(add_video_form) }}
    </div>
{% endblock %}